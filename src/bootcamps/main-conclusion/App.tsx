// App.tsx is reserved for future top-level providers.
// Current entry: src/main.tsx → RouterProvider(router) → WorkspaceShell.
// Routes are defined in src/routes.tsx.
export default function App() {
  return null;
}
